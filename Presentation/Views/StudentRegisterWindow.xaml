<?xml version="1.0" encoding="utf-8"?>
<Window x:Class="Presentation.Views.StudentRegisterWindow" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" Title="Student registration" Height="390" Width="400" WindowStartupLocation="CenterOwner">
  <Grid Margin="28">
      <Grid.RowDefinitions>
          <RowDefinition Height="Auto"/>
          <RowDefinition Height="Auto"/>
          <RowDefinition Height="Auto"/>
          <RowDefinition Height="Auto"/>
          <RowDefinition Height="Auto"/>
          <RowDefinition Height="*"/>
      </Grid.RowDefinitions>
    <TextBlock Text="Full name"/>
      <TextBox Grid.Row="1" Text="{Binding FullName, UpdateSourceTrigger=PropertyChanged}" Height="30"/>
    <TextBlock Grid.Row="2" Text="Email"/>
      <TextBox Grid.Row="3" Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}" Height="30"/>
    <StackPanel Grid.Row="4">
        <TextBlock Text="Password"/>
        <PasswordBox PasswordChanged="PasswordBox_PasswordChanged" Height="30"/>
        <TextBlock Text="{Binding Error}" Foreground="IndianRed"/>
    </StackPanel>
    <Button Grid.Row="5" Content="Register" Command="{Binding RegisterCommand}" HorizontalAlignment="Right" VerticalAlignment="Bottom"/>
  </Grid>
</Window>
