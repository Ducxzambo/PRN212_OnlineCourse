<UserControl x:Class="Presentation.Views.StudentProgressView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="White" Padding="20">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Title -->
        <TextBlock Grid.Row="0" Text="Tiến độ học tập" FontSize="20" FontWeight="Bold" Margin="0,0,0,15"/>

        <!-- Error Message -->
        <Border Grid.Row="0" Background="#FFE6E6" Padding="10" Margin="0,0,0,10"
                Visibility="{Binding ErrorMessage, Converter={StaticResource StringToVisibilityConverter}}">
            <TextBlock Text="{Binding ErrorMessage}" Foreground="#C00" FontSize="12"/>
        </Border>

        <!-- Progress List -->
        <ScrollViewer Grid.Row="1">
            <ItemsControl ItemsSource="{Binding Enrollments}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Background="#F9F9F9" BorderBrush="#DDD" BorderThickness="1"
                                Padding="15" Margin="0,0,0,10" CornerRadius="5">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>

                                <TextBlock Grid.Row="0" Text="{Binding Course.Title}" FontSize="14" FontWeight="Bold"/>

                                <TextBlock Grid.Row="1" FontSize="12" Foreground="#666" Margin="0,5,0,5">
                                    <Run Text="Trạng thái: "/>
                                    <Run Text="{Binding Status}"/>
                                </TextBlock>

                                <Grid Grid.Row="2">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="100"/>
                                    </Grid.ColumnDefinitions>

                                    <ProgressBar Grid.Column="0" Value="{Binding Progress}" Maximum="100" 
                                                 Height="15" Background="#EEE" Foreground="#3498DB"/>

                                    <TextBlock Grid.Column="1" Text="{Binding Progress, StringFormat={}{0}%}" 
                                               Margin="10,0,0,0" VerticalAlignment="Center" FontWeight="Bold"
                                               Foreground="#3498DB"/>
                                </Grid>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

        <!-- Loading Indicator -->
        <StackPanel Grid.Row="1" HorizontalAlignment="Center" VerticalAlignment="Center"
                   Visibility="{Binding IsLoading, Converter={StaticResource StringToVisibilityConverter}}">
            <TextBlock Text="Đang tải..." Foreground="#999"/>
        </StackPanel>
    </Grid>
</UserControl>
