<Window x:Class="Presentation.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Online Course Management - Đăng Nhập" Height="400" Width="420"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize">
    <Grid Margin="32">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Online Course Management" Style="{StaticResource HeadingText}"/>
        <TextBlock Grid.Row="1" Text="Đăng nhập để tiếp tục" Style="{StaticResource SubHeadingText}" Margin="0,4,0,20"/>

        <!-- Username Field -->
        <TextBlock Grid.Row="2" Text="Tên tài khoản hoặc Email" Style="{StaticResource FieldLabel}"/>
        <TextBox Grid.Row="3" x:Name="UsernameTextBox" Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}"
                 Height="34" Margin="0,0,0,4">
            <TextBox.InputBindings>
                <KeyBinding Key="Return" Command="{Binding LoginCommand}"/>
            </TextBox.InputBindings>
        </TextBox>

        <!-- Password Field -->
        <TextBlock Grid.Row="4" Text="Mật khẩu" Style="{StaticResource FieldLabel}"/>
        <PasswordBox Grid.Row="5" x:Name="PasswordBox" Height="34" Margin="0,0,0,4"/>

        <!-- Error Message -->
        <TextBlock Grid.Row="6" Text="{Binding ErrorMessage}"
                   Style="{StaticResource ErrorText}"
                   Visibility="{Binding ErrorMessage, Converter={StaticResource StringToVisibilityConverter}}"/>

        <!-- Login Button -->
        <StackPanel Grid.Row="7" VerticalAlignment="Bottom" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Đăng nhập" Command="{Binding LoginCommand}" IsDefault="True" MinWidth="100"/>
        </StackPanel>
    </Grid>
</Window>
