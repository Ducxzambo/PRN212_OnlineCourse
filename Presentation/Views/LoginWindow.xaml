<Window x:Class="Presentation.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Đăng nhập" Height="390" Width="420"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize">
    <Grid Margin="32">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Online Course Management" Style="{StaticResource HeadingText}"/>
        <TextBlock Grid.Row="1" Text="Đăng nhập để tiếp tục" Style="{StaticResource SubHeadingText}" Margin="0,4,0,20"/>

        <TextBlock Grid.Row="2" Text="Email" Style="{StaticResource FieldLabel}"/>
        <TextBox Grid.Row="3" Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}"
                 Height="34" Margin="0,0,0,4">
            <TextBox.InputBindings>
                <KeyBinding Key="Return" Command="{Binding LoginCommand}"/>
            </TextBox.InputBindings>
        </TextBox>
        <TextBlock Grid.Row="4" Text="Mật khẩu" Style="{StaticResource FieldLabel}" Margin="0,8,0,4"/>
        <PasswordBox Grid.Row="5" PasswordChanged="PasswordBox_PasswordChanged" Height="34" Margin="0,0,0,4">
            <PasswordBox.InputBindings>
                <KeyBinding Key="Return" Command="{Binding LoginCommand}"/>
            </PasswordBox.InputBindings>
        </PasswordBox>
        <TextBlock Grid.Row="6" Text="{Binding ErrorMessage}"
                   Style="{StaticResource ErrorText}"
                   Visibility="{Binding ErrorMessage, Converter={StaticResource StringToVisibilityConverter}}"/>

        <StackPanel Grid.Row="7" VerticalAlignment="Bottom" Orientation="Horizontal" HorizontalAlignment="Right">
            <TextBlock VerticalAlignment="Center" Margin="0,0,12,0">
                <Run Text="Student chưa có tài khoản? "/>
                <Hyperlink Click="RegisterStudent_Click">Register</Hyperlink>
            </TextBlock>
            <Button Content="Đăng nhập" Command="{Binding LoginCommand}" IsDefault="True" MinWidth="100"/>
        </StackPanel>
    </Grid>
</Window>
