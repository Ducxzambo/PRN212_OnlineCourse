<UserControl x:Class="Presentation.Views.AdminLessonListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,16">
            <Button Content="&#8592; Quay lại" Command="{Binding BackCommand}" Style="{StaticResource SecondaryButton}" Margin="0,0,12,0"/>
            <StackPanel>
                <TextBlock Text="{Binding Course.Title}" Style="{StaticResource HeadingText}"/>
                <TextBlock Text="Danh sách bài học (chỉ xem)" Style="{StaticResource SubHeadingText}" Margin="0,4,0,0"/>
            </StackPanel>
        </StackPanel>

        <Button Grid.Row="1" Content="Làm mới" Command="{Binding RefreshCommand}" Style="{StaticResource SecondaryButton}" HorizontalAlignment="Right" Margin="0,0,0,12"/>

        <DataGrid Grid.Row="2" ItemsSource="{Binding Lessons}" MinHeight="160" MaxHeight="480">
            <DataGrid.Columns>
                <DataGridTextColumn Header="#" Binding="{Binding OrderIndex}" Width="45"/>
                <DataGridTextColumn Header="Tên bài học" Binding="{Binding Title}" Width="2*"/>
                <DataGridTextColumn Header="Nội dung" Binding="{Binding Content}" Width="2*"/>
                <DataGridTextColumn Header="Link video" Binding="{Binding VideoUrl}" Width="2*"/>
                <DataGridTextColumn Header="Thời lượng (phút)" Binding="{Binding DurationMinutes}" Width="120"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
